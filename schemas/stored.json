{

    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "Schema for an xAPI Stored Property",
    "id":"http://localhost:8080/stored-schema.json",

    "type": "object",

    "additionalProperties": false,

    "properties": {
        "stored": { "$ref": "#/definitions/stored" }
    },

    "definitions": {

        "stored": {
            "type": "string",
            "format": "date-time"
        }

    }
}
