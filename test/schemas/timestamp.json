{

    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "Schema for an xAPI Timestamp Property",
    "id":"http://localhost:8080/timestamp-schema.json",

    "type": "object",

    "additionalProperties": false,

    "properties": {
        "timestamp": { "$ref": "#/definitions/timestamp" }
    },

    "definitions": {

        "timestamp": {
            "type": "string",
            "format": "date-time"
        }

    }
}
